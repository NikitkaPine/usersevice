spring:
  application:
    name: user-sevice

  datasource:
    url: jdbc:postgresql://localhost:5432/userdb
    username: postgres
    password: GHD263ghjk!
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

  task:
    scheduling:
      enabled: true

server:
  port: 8080

jwt:
  secret: "your-very-long-and-secure-secret-key-change-this-in-production-minimum-256-bits"
  expiration: 120000
  refresh-expiration: 86400000
storage:
  avatar-path: ./uploads/avatars

logging:
  level:
    com.example.usersevice: DEBUG
    org.springframework.security: DEBUG